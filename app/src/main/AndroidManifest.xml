<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialThemes">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Diagonal Widget Provider (2x2) -->
        <receiver
            android:name=".widget.DiagonalWidgetProvider"
            android:exported="true"
            android:label="@string/widget_diagonal_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.iatb.materialthemes.ACTION_WIDGET_TICK" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/diagonal_widget_info" />
        </receiver>

        <!-- Diagonal 4x3 Multi-block Forecast Widget Provider -->
        <receiver
            android:name=".widget.Diagonal4x3WidgetProvider"
            android:exported="true"
            android:label="@string/widget_diagonal_4x3_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.iatb.materialthemes.ACTION_WIDGET_TICK" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/diagonal_4x3_widget_info" />
        </receiver>

        <!-- Organic Shapes Widget Provider (2x2) -->
        <receiver
            android:name=".widget.OrganicWidgetProvider"
            android:exported="true"
            android:label="@string/widget_organic_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/organic_widget_info" />
        </receiver>

        <!-- Organic Wide Widget Provider (4x2) -->
        <receiver
            android:name=".widget.OrganicWideWidgetProvider"
            android:exported="true"
            android:label="@string/widget_organic_4x2_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/organic_4x2_widget_info" />
        </receiver>

        <!-- Scallop & Starburst Widget Provider (2x2) -->
        <receiver
            android:name=".widget.ScallopWidgetProvider"
            android:exported="true"
            android:label="@string/widget_scallop_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/scallop_widget_info" />
        </receiver>

        <!-- Scallop Wide Widget Provider (4x2) -->
        <receiver
            android:name=".widget.ScallopWideWidgetProvider"
            android:exported="true"
            android:label="@string/widget_scallop_4x2_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/scallop_4x2_widget_info" />
        </receiver>
    </application>

</manifest>