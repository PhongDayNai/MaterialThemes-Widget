<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialThemes">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Diagonal Widget Provider -->
        <receiver
            android:name=".widget.DiagonalWidgetProvider"
            android:exported="true"
            android:label="@string/widget_diagonal_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/diagonal_widget_info" />
        </receiver>

        <!-- Organic Shapes Widget Provider -->
        <receiver
            android:name=".widget.OrganicWidgetProvider"
            android:exported="true"
            android:label="@string/widget_organic_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/organic_widget_info" />
        </receiver>

        <!-- Scallop & Starburst Widget Provider -->
        <receiver
            android:name=".widget.ScallopWidgetProvider"
            android:exported="true"
            android:label="@string/widget_scallop_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/scallop_widget_info" />
        </receiver>
    </application>

</manifest>